@namespace("org.guild.adventurer")
protocol AdventurerDiedProtocol {
  /** An adventurer dies (rocks fall). Rare event. */
  record AdventurerDied {
    string adventurer_id;
    string cause;
    timestamp_ms died_at;
  }
}
